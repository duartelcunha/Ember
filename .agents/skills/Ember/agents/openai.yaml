interface:
  display_name: "Ember"
  short_description: "Repo-specific patterns and workflows for Ember"
  default_prompt: "Use the Ember repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true