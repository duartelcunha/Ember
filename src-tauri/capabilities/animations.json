{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "animations",
  "description": "Only animation lifecycle commands.",
  "windows": [
    "splash",
    "startup_anim",
    "quit_anim"
  ],
  "permissions": [
    "core:default",
    "allow-close-splash",
    "allow-finalize-quit"
  ]
}
